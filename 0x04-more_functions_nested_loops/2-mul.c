#include "main.h"
/**
 * mul - function that multiplicate 2 integers
 * @a: first integer
 * @b: second integer
 * Return: always 0 (success)
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
